import 'package:flutter/material.dart';

class Frame4 extends StatelessWidget {
  const Frame4({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Frame 4")),
    );
  }
}