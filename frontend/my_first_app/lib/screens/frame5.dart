import 'package:flutter/material.dart';

class Frame5 extends StatelessWidget {
  const Frame5({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Frame 5")),
    );
  }
}